import { FormVehicleDetails } from "@/components"
import { Layout } from "@/themes"

export const FormVehicleDetail = () => {
  return (
    <Layout>
      <FormVehicleDetails/>
    </Layout>
  )
}
