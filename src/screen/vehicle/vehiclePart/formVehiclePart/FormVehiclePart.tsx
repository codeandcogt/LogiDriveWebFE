import { FormVehicleParts, Navbar } from "@/components"
import { Layout } from "@/themes"

export const FormVehiclePart = () => {
  return (
    <Layout>
      <Navbar/>
      <FormVehicleParts/>
    </Layout>
  )
}
