import { ListUser, Navbar } from "@/components"
import { Layout } from "../../themes"

export const User = () => {
  return (
    <Layout>
      <Navbar/>
        <ListUser/>
    </Layout>
  )
}
