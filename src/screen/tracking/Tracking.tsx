import { MapView, Navbar } from "@/components"
import { Layout } from "../../themes"

export const Tracking = () => {
  return (
    <Layout>
        <Navbar/>
        <MapView/>
    </Layout>
  )
}
