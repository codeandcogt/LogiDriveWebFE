import { FormAssigments, Navbar } from "@/components"
import { Layout } from "@/themes"

export const FormAssigment = () => {
  return (
    <Layout>
      <Navbar/>
      <FormAssigments/>
    </Layout>

  )
}
