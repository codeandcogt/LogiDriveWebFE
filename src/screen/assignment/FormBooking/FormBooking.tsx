import { FormBookings, Navbar } from "@/components"
import { Layout } from "@/themes"

export const FormBooking = () => {
  return (
    <Layout>
      <Navbar/>
      <FormBookings/>
    </Layout>
  )
}
